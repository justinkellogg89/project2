<!DOCTYPE html>
<html lang="en">

<head>
  <title>Wild Kards</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
  <link href="/stylesheets/style.css" rel="stylesheet">
</head>

<body>
  <div class="container">
    <div class="row">
      <div class="col-md-6 offset-md-3">
        <blockquote class="blockquote text-center">
          <h2 class="mb-0 display-1">{{quote.body}}</h2>
          <h1 class="display-4">
            <footer class="blockquote-footer"><cite title="Source Title">{{quote.author}}
          </h1></cite></footer>
        </blockquote>
      </div>
    </div>
    <div class="row">
      <div class="col-md-6 offset-md-3">
        <form id="comForm">
          <div class="form-group">
            <textarea placeholder="Write your comment here!" class="form-control" rows="3" id="com"
              data-id="{{quote.id}}"></textarea>
          </div>
            <button type="submit" class="btn btn-success submit"
              class="mw-100 offset-md-3">Submit</button>
        </form>
      </div>
    </div>
    <div class="row">
        {{#if comments}}
        <div class="col-md-6 offset-md-3 w-75 p-3"
          style="background-color: white; margin-top: 40px; padding-right: 50px; border: 2px solid gray; margin-bottom: 20px;">
        <h3 style="font-weight: bold;">Comments:</h3>
        {{#each comments}}
        <div class="col-md-12 col-md-offset-1">
          <span>{{formatDate createdAt}}</span>
          <h4 style="font-weight: bold;">{{ this.User.email }}: </h4>
          <h5> {{ comment }} </h5>
        </div>
        {{/each}}
      </div>
      {{else}}
      <div class="col-md-6 offset-md-3 w-75 p-3"
      style="background-color: white; margin-top: 40px; padding-right: 50px; border: 2px solid gray; margin-bottom: 20px;">
        <div class="col-md-12 col-md-offset-1">
          <h4>No Comments yet!</h4>
        </div>
      </div>
      {{/if}}
    </div>
  </div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script type="text/javascript" src="/js/comment.js"></script>